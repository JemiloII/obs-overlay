{
  "name": "twitch-overlay",
  "version": "0.1.0",
  "private": true,
  "description": "Custom Twitch chat overlay for OBS browser source",
  "scripts": {
    "dev": "pnpm --parallel --filter @twitch-overlay/server --filter @twitch-overlay/client dev",
    "build": "pnpm --recursive build",
    "type-check": "pnpm --recursive type-check"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0"
}
